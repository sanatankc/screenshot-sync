# Repo Working Rules

## Commit Timing
When working through tasks in this repo, do not commit immediately after finishing a task.
Leave the task changes uncommitted for review.
Create the commit only when starting the next task, so the previous task has a review window.
